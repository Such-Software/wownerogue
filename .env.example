# Single-player dungeon mode. On by default; set false for an instance that only runs
# other modes (e.g. Tavern-only).
SOLO_ENABLED=true
# Real-time multiplayer match mode (not yet available).
MULTIPLAYER_ENABLED=false
